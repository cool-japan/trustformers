# TrustformeRS TODO

## Status as of 2026-08-24

- **Version**: 0.2.1 (branch `0.2.1`, unreleased). Workspace has 10 members — `trustformers-core`, `trustformers-tokenizers`, `trustformers-models`, `trustformers-optim`, `trustformers-training`, `trustformers` (umbrella), `trustformers-wasm`, `trustformers-mobile`, `trustformers-debug`, `trustformers-serve`. `trustformers-py`, `trustformers-c`, and `trustformers-serve/clients/rust` are deliberately excluded from the Cargo workspace (each has its own standalone manifest); `trustformers-js` is also excluded (WASM/JS bindings, not a Cargo package).
- **Size** (`tokei` 12.1.2, 2026-08-24, repo root, `target`/`node_modules`/`.claude` excluded): 3,046 Rust files, 1,527,609 total lines, 1,275,615 lines of code. (2026-08-18's count was 3,029 files / 1,530,223 / 1,277,590 — file count rose from this pass's file-splitting work while total lines fell slightly, net of the splits' boilerplate against several packages' deletion of dead/fabricated code.)
- **Build**: `cargo check --workspace --all-targets` reported GREEN (default features) immediately before this documentation pass, per the orchestrator.
- **Tests (workspace baseline, as reported to this pass)**: `cargo nextest run --workspace` (default features) — 20,629 passed / 43 skipped / 0 failed, measured by the orchestrator minutes before this pass started. This documentation pass did not independently re-run the suite (see "How to read this file"). As before, this is a default-features number and excludes suites gated behind non-default features (e.g. `trustformers-models/tests/export_round_trip.rs` needs `gpt2,bert,llama,mistral,t5` together; `trustformers-mobile`'s `advanced_privacy_mechanisms` tests need `on-device-training`) — it undercounts total coverage, not a ceiling.
- **`cargo deny check bans`**: PASS as of 2026-08-18, not re-run this pass. Nothing in this wave's work is evident bans-relevant (only removals — `candle-core`/`zip`, `ffmpeg-next`, four Azure SDKs, `google-cloud-gax`, `google-cloud-functions-v2`, `lambda_runtime` — no new dependency was added that would appear on the banned list).
- **`cargo deny check licenses`**: PASS as of 2026-08-18, not re-run this pass.
- **`cargo deny check advisories`**: **FAIL**, 7 findings as of 2026-08-18, not re-run this pass — but the root cause was independently reconfirmed today by reading the manifest directly: `trustformers-serve/Cargo.toml` still declares `aws-sdk-sqs`, `aws-sdk-sagemaker`, `aws-sdk-sagemakerruntime`, `aws-sdk-lambda`, `aws-sdk-cloudwatch` (all `.workspace = true`), which is the dependency chain that pulls the old `rustls 0.21`/`rustls-webpki 0.101.7` stack these 7 advisories are rooted in. See P0 #1.

## How to read this file

This is this file's third generation. The version before 2026-08-18 had accumulated an 18-item stale-claim list (contradictory test counts, checked-off boxes for a feature that no longer existed, a stale version header, ✅-marked hardware backends that were empty feature flags) and was rewritten from scratch against the tree as it stood then. That 2026-08-18 version in turn described a "Wave 4" whose work packages had mostly self-reported FAILED, with only `serve-honesty` reporting completion — but noted that inspecting the tree told a more specific story than the self-reports did.

Today (2026-08-24) a resumed Wave 4 ran across nine work packages (`serve-rm`, `serve-honesty`, `umbrella-warnings`, `splits`, `py-pipelines`, `manifests`, `models-loaders`, `core-polish`, `mobile-audit`) and closed a large fraction of what the 2026-08-18 version listed as open. Every claim below was checked again today by reading the current code (grep, `wc -l`, `tokei`, and two targeted `cargo check` runs) rather than by trusting either the old file or any package's own self-report — and in two places (see the Metal item under P0, and the file-size item under P1) a package's own count of its own remaining work turned out to be off from what a direct grep finds today; both are reported with the freshly-measured number, not the reported one.

## P0 — currently failing gates

1. **`cargo deny check advisories` fails with 7 findings** (last actually run 2026-08-18; root cause reconfirmed present today, see Status above): `opentelemetry-jaeger` unmaintained, `paste` unmaintained, an `h2` unbounded-empty-DATA-frames vulnerability, an RSA "Marvin Attack" timing side-channel, two `rustls` name-constraints-acceptance vulnerabilities, and a `rustls-webpki` 0.101.7 CRL-parsing reachable panic (RUSTSEC-2026-0104) — all reached through `trustformers-serve`'s `aws-sdk-{sqs,sagemaker,sagemakerruntime,lambda,cloudwatch}` dependencies' older `rustls`/`rustls-webpki` stack. `cargo deny check bans` and `check licenses` both still pass as of their last run.
2. **`cargo check -p trustformers-models --features metal,gpt2` fails with 15 compiler errors**, verified today with a targeted build (not part of the default-feature workspace baseline, so it did not show up in the GREEN check above). This is a regression surfaced by this wave's own (correct, in-progress) work: `trustformers-core`'s `Tensor::Metal` now stores a `MetalBufferHandle` instead of a bare `BufferId` (`core-polish`'s Mission 5), but `trustformers-models/src/gpt2/model/{model_blocks.rs,model_core.rs,model_ops.rs}` still writes against the old shape. Precisely: 5× `E0560` ("no field named `buffer_id`") at the `MetalTensorData { .. }` construction-literal sites (`model_ops.rs:240`; `model_blocks.rs:779,784,792,807`) and 10× `E0615` ("method, not a field") at bare `.buffer_id` reads (`model_blocks.rs:698,708,709,807,862`; `model_core.rs:342,824,922,967`; `model_ops.rs:224`) — `core-polish`'s own handoff estimated "5 + 6"; a full grep plus this compile run finds 5 + 10, which is also exactly 15. Fix: convert these 15 sites to the `MetalBufferHandle` accessor API the rest of the Metal path already uses (see `trustformers-core/src/gpu_ops/metal/types.rs`'s doc comment for the pattern).

## P1 — verified open work

### trustformers-serve
- `serve-honesty`'s own account of its remaining scope (attributed to that package, not independently recounted this pass): 14 marker sites for no-op strategy tracking plus invented optimization insights; 14 more marker sites (`PredictionModel` itself was fixed, the rest of that batch was not); 31 marker sites across alerting/trend/recommendation start-stop no-ops and canned insights; ~17 more across `analytics/`, `notifications/`, `threshold/`, `optimization/`, `buffer/`; `SystemResourceCollector` and `ProcessMetricCollector` still return fabricated metrics; ~30 files outside the four subtrees cleaned this wave (`resource_management`, `performance_optimizer`, `caching`, `batching` module roots) still carry `#![allow(dead_code)]`.
- `serve-rm`'s own account of what it left behind: two fabrications inside `lib.rs` (the file it had to migrate to re-point the unprefixed resource-management exports) that it did not fix because doing so was out of its ownership as a source-tree package that pass — not yet identified by file/line in this pass; and the sub-managers under `resource_management/` that the unprefixed names now point at (`gpu_manager/`, `temp_dir_manager/`, etc.) have not had a dedicated correctness audit of their own — `serve-rm`'s scope was re-pointing the public names and adding a regression guard for the unprefixed surface, not auditing every sub-manager behind it.
- The previous version of this file listed `resource_manager/` (the placeholder tree, fabricating ports/paths/connections/GPU stats/efficiency) as still exported under the unprefixed `ResourceManagementSystem` name. **Resolved**: the placeholder tree is deleted outright (confirmed: `trustformers-serve/src/resource_manager/` no longer exists); `lib.rs` now re-exports `ResourceManagementSystem` directly from the real `resource_management` module, with `ModularResourceManagementSystem` kept as a compatibility alias for the same type, and a comment in `lib.rs` explaining the history. See "Recently resolved."

### trustformers-core
- `gpu_ops/metal/types.rs`'s new `MetalBufferHandle` (RAII, refcounted, releases its cache entry on last drop) is wired into `Tensor::Metal`'s own storage and into every GPU-to-GPU result site trustformers-core itself owns (`gelu`, `Tensor::add`, all three `layernorm_gpu_to_gpu` sites, `Linear::forward`'s matmul+bias-add, `tensor_to_f32`'s download read). The 15 sites in `trustformers-models/src/gpt2` that still construct/read the old bare-`buffer_id` shape are tracked as P0 #2 above, not here, since they live in a different crate.
- Separately: the CPU-input/GPU-weight `LayerNorm` fallback still uploads `input_buffer_id` as a `Pinned` buffer and never releases it (documented by `core-polish` as a pre-existing, separate leak from the RAII work above, around `trustformers-core/src/gpu_ops/metal/*.rs:487-492` — not re-verified independently this pass).
- `kernels/riscv_impl.rs`, `kernels/xla_impl.rs`, `kernels/oneapi_impl.rs`: the `riscv`/`xla`/`oneapi` Cargo features are all still `= []` (zero dependencies), confirmed today. Verified today, module-comment by module-comment (the previous version of this file asked for exactly this check and it had not been done): `riscv_impl.rs`'s module doc comment is honest and explicit — it states outright that it does not execute real RVV instructions and that results always come from a scalar CPU fallback. `xla_impl.rs` and `oneapi_impl.rs` are honest **at the method level** — every public method on `XlaClient`/`OneApiBackend` and friends returns a structured error naming exactly what isn't linked ("no XLA runtime is linked", "no SYCL/oneDNN/oneMKL runtime is linked"), never a fabricated result — but their module-level (top-of-file) doc comments, unlike `riscv_impl.rs`'s, say only "provides integration with Google's XLA compiler" / "Intel's oneAPI unified programming model" with no disclaimer that nothing is actually linked; a reader who stops at the top of either file would come away with the wrong impression. Small doc-polish item, not a behavior bug — the code itself never fabricates.

### trustformers-py
- `src/pipelines.rs` no longer exists as a single file; the module is now `src/pipelines/` (`mod.rs` + `scoring.rs`). Verified today: the fabricated `text-generation` `__call__` (`format!("{text} [Generated continuation]")` / hardcoded `score: 0.95`) is gone from the live code — the two remaining `let _ = (args, kwargs)` sites in `pipelines/mod.rs` are in `PyTokenClassificationPipeline::new` and `PyQuestionAnsweringPipeline::new`, whose constructors now unconditionally return a structured `PyNotImplementedError` (`span_pipeline_unavailable`, explaining that this crate's tokenizers don't produce character-level offset mappings) instead of inventing a `B-PER`/`"John"` entity or an `"Example answer"`/`score: 0.85` response. This is an honest refusal, not leftover fabrication.
- Real token-classification and question-answering pipelines (currently refuse rather than implement — blocked on `trustformers-tokenizers` producing an offset mapping), `batch_encode_plus` text-pair handling, and a `pytest` smoke run are all still open, per `py-pipelines`'s own deferred list; not independently re-verified this pass.
- `Cargo.toml:77` still depends on the `trustformers` umbrella crate — unchanged from before; do not remove without the before/after `cargo tree -e features -p trustformers-core` diff the prior wave's handoff notes describe.
- `pyo3` is still pinned to `0.28` (`scirs2-numpy`/`scirs2-core` pinned to `0.5.1`, with a dated comment explaining the `pyo3 0.29` conflict) — confirmed today; the `0.28`→`0.29` migration itself is explicitly deferred by `py-pipelines`, not started.
- **New, found today, outside `py-pipelines`'s scope (pipelines, not training)**: `src/training.rs`'s `PyTrainer::train()` is still fabricated — its own comment reads "In a real implementation, we'd run the training loop here / For now, return a mock training result", and it unconditionally returns `train_loss: 0.5`, `total_steps: 1000` regardless of the actual data or configuration passed in. `PyTrainer`/`PyTrainingArguments` are real, registered pyclasses (`m.add_class::<PyTrainer>()`/`<PyTrainingArguments>()` in `lib.rs`), so Python code can construct and call `.train()` on them today and silently receive these two fabricated numbers back. Needs the same honesty treatment the pipelines got this wave: either a real training loop or a structured refusal, not a hardcoded result.

### File-size policy (files >2000 lines; measured with `wc -l`, verified 2026-08-24 with a full workspace scan)
All six files this section previously listed are now split into directory modules (`splits` completed its whole scope), confirmed today: `trustformers-mobile/src/inference/` (8 files, largest `engine.rs` at 1,129 lines; `parse_safetensors` correctly stayed `pub(crate)` and `trustformers-mobile/src/wasm.rs` still calls it via `crate::inference::MobileInferenceEngine::parse_safetensors`), `trustformers-training/src/data_pipeline/` (9 files), `trustformers-mobile/src/profiler/` (10 files), `trustformers-training/src/auto_parallelism/` (6 files, largest `selector.rs` at 1,355 lines), `trustformers-mobile/src/crash_reporter/` (7 files), `trustformers/src/hub_offline_packs/` (6 files).

A fresh full-workspace `find … | xargs wc -l | awk '$1>2000'` (not just a re-check of the old list) turns up three files the previous version of this file never named, all in the `trustformers` umbrella crate:
- `trustformers/src/hub_ui.rs` — 2026 lines
- `trustformers/src/hub.rs` — 2013 lines
- `trustformers/src/pipeline/conversational/summarization.rs` — 2001 lines

None of the three were touched by `splits` (whose named scope was the original six). Split with `splitrs` or manual module extraction.

### trustformers-mobile
- The previous version of this file listed `src/battery.rs:922,938,953,1038` as hardcoding `power_consumption_mw` (`Some(2500.0)`/`Some(2200.0)`/`Some(1800.0)`). **Resolved**, verified today: the field is `Option<f32>`, populated from a real `power_supply` sysfs read (`power_now`) or left `None` when unavailable; the only remaining `2500.0`/`2200.0`/`1800.0` text in the file is inside doc comments and a test describing the old, now-fixed behavior for regression purposes.
- The previous version flagged `src/mobile_performance_profiler/` (~25 files) as never having had a dedicated fabrication audit, noting a same-pass spot check found "in a real implementation"/"placeholder"/"TODO"/"fake" exactly once across all 25 files. Re-running that identical grep today finds **zero** hits — resolved as a side effect of `mobile-audit`'s broader sweep, though (as before) a single grep pattern across 25 files is not equivalent to reading them.
- `react-native-plugin/` → `react-native-example/`: renamed, confirmed today (`ls trustformers-mobile/react-native-plugin` → no such directory; `react-native-example/` exists with the same `TrustformersCompleteExample.tsx` + README). `trustformers-mobile/README.md` and `trustformers-mobile/TODO.md` already document this rename in detail (both dated 2026-08-24) and are not duplicated here.
- `src/tflite_nnapi_delegate.rs`: the previous version of this file said this was orphaned, not mounted from `lib.rs`. That claim is now stale — `pub mod tflite_nnapi_delegate;` is present in `trustformers-mobile/src/lib.rs:147` today. Whether this was already true on 2026-08-18 and simply mis-recorded, or became true since, was not determined; either way it is not open work today.

### Dependency / manifest hygiene
- `examples/grpc-server/Cargo.toml`: **resolved**. The previous version of this file said it was pinned to `tonic 0.12`/`prost 0.13` against a workspace on `tonic 0.14.6`/`prost 0.14`. Verified today: it now declares `tonic = "0.14.6"`, `tonic-prost = "0.14.6"`, `prost = "0.14"`, `tonic-prost-build = "0.14.6"` (tonic 0.14 moved codec generation out of `tonic-build`), with dated comments explaining each change; the service, model registry, env config, error enum, README, Dockerfile, and docker-compose were all migrated to match (`manifests`'s account; not independently re-read line-by-line this pass beyond the manifest and README).
- `serde_yaml`: **resolved** for both real workspace members the previous version of this file named. Checked precisely today (dependency-key grep, not a substring match on the crate name, which also matches `serde_yaml_ng`): `trustformers/Cargo.toml` and `trustformers-models/Cargo.toml` both now declare `serde_yaml_ng.workspace = true`; no `.rs` file anywhere in the four crates checked (`trustformers`, `trustformers-models`, `trustformers-tokenizers`, `trustformers-serve`) calls `serde_yaml::` anymore. The one remaining literal `serde_yaml = "0.9"` is in `trustformers-c/Cargo.toml` — that crate is deprecated, excluded from the Cargo workspace, and need not compile per policy, so this is an accepted exception rather than open work.
- **New, found today**: `examples/server/Cargo.toml` has the same latent defect `examples/grpc-server/Cargo.toml` had before this wave fixed it. It declares `version.workspace = true` / `edition.workspace = true` while being excluded from the repository workspace (root `Cargo.toml`'s `exclude` list) and defines no `[workspace]` table of its own — `cargo` invoked from inside `examples/server/` cannot resolve those keys (verified: the file is syntactically valid TOML, checked with `python3 -c "import tomllib; tomllib.load(...)"`, but has no workspace to inherit `version`/`edition` from, the same class of problem `serve/clients/rust/Cargo.toml` and `examples/grpc-server/Cargo.toml` both had before their fixes this wave). Fix the same way: give it a real `[package]` `version`/`edition`, or its own `[workspace]` table.
- `cargo clippy -p trustformers --all-targets --all-features -- -D warnings`: still not re-run this pass (a clippy run is outside a documentation remit). ~205 warnings were reported as of the last run before 2026-08-18's version of this file, driven by the blanket `#![allow(dead_code)]` removal from `trustformers/src/lib.rs`. `umbrella-warnings` closed a specific, named list of dead-code items this wave (see "Recently resolved"), which should have reduced that count, but by how much was not re-measured.

## P2 — smaller open items

- `trustformers-optim`: the previous version of this file's P2 entry here was only half right, corrected today. Still accurate: no Shampoo optimizer exists (`rg -i shampoo` across `src/` finds only an incidental mention inside a `lora_rite.rs` doc comment listing algorithms it's evaluated against, not an implementation). Now stale, and removed: `genie_stub`/`sofo_stub`/`lora_rite_stub` do not exist under those names anywhere in `trustformers-optim/src/` today (checked by filename and by symbol grep — zero hits); `StatefulOptimizer` (`traits.rs`) already provides default `save_state`/`load_state` methods with a round-trip regression test; `AdamW4bit` already exists (`quantized_advanced.rs`, real `Optimizer`/`StatefulOptimizer` impls, 3 tests). None of these three were touched by this wave's named packages, so either they were already fixed before 2026-08-18 and mis-recorded, or the original claim was simply wrong.
- `trustformers/examples/conversational_ai.rs.disabled`: **resolved** — the file is now `trustformers/examples/conversational_ai.rs` (no `.disabled` suffix), confirmed today.
- `trustformers-training`: still no `benches/` directory, confirmed today.
- `trustformers-core/benches/tensor_operations.rs`: unchanged, confirmed today — three registered groups still measure only `tensor.clone()`, and two groups (`complex_placeholder`, `quantization_placeholder`) are explicitly labelled placeholders in their own source comments. Nothing benchmarks attention, KV-cache, the decode loop, or serve-side batching.
- `trustformers-serve/Cargo.toml`: `rust-version` is still set via `workspace = true`, but the crate still carries more literal version pins than any other member — not audited pin-by-pin this pass (unchanged from before).

## Recently resolved — verified 2026-08-24, this pass

Each item below was independently confirmed today by reading the current code (not by trusting a package's self-report). Several directly supersede items the 2026-08-18 version of this file still carried as open.

- **`trustformers-serve`'s `resource_manager/` placeholder tree is deleted**, not merely deprecated — the directory no longer exists. The unprefixed `ResourceManagementSystem` name now resolves to the real, tested `resource_management` module directly (`ModularResourceManagementSystem` kept only as a type alias for compatibility); `lib.rs` (the tree's only in-crate consumer) was migrated to match, with 4 new regression tests guarding the unprefixed surface.
- **`trustformers-serve/clients/rust` now builds standalone**: its manifest gained its own `[workspace]` table and literal dependency versions (`reqwest` moved from the removed `rustls-tls` feature name to `rustls`; `form` added for an OAuth2 POST body), and the crate now implements the OAuth2 client-credentials grant natively (no `oauth2` dependency) with hermetic regression tests, on top of fixing 11 pre-existing compile errors. Its README was rewritten — the version at 2026-08-18 documented an API the code did not implement.
- **Root `Cargo.toml`'s 7 orphaned `[workspace.dependencies]` entries are gone.** `azure_core`, `azure_identity`, `azure_mgmt_machinelearningservices`, `azure_mgmt_web`, `google-cloud-functions-v2`, `google-cloud-gax`, `lambda_runtime` are no longer declared anywhere in the root manifest (confirmed by grep: zero matches), closing what was P0 #1 in the 2026-08-18 version of this file. `trustformers-tokenizers`'s `workspace_dependency_table_has_no_unused_entries` hygiene test should now pass as a result (not independently re-run this pass; see Status).
- **`examples/grpc-server`** migrated from `tonic 0.12`/`prost 0.13` to `tonic 0.14.6`/`tonic-prost 0.14.6`/`prost 0.14`, with the service rewritten against the real proto and the real `AutoModel` API, a model registry that measures instead of estimating, real env-based configuration, gRPC reflection v1/v1alpha, and a de-fabricated README/Dockerfile/docker-compose.
- **`trustformers-models` checkpoint loading**, continuing what the 2026-08-18 version of this file already recorded for `roberta`/`albert`/`fnet`/`nemotron`/etc. (note: **not** `s4` — see below, that architecture moved the other direction this pass): `fnet`'s task wrappers no longer silently drop their prediction head (`FNetLMPredictionHead` is now real); `roberta` and `albert`'s four task wrappers each now bind the encoder *and* the head, instead of dropping the head; `bind_head_linear`/`bind_head_layer_norm` were hoisted out of `bert/tasks.rs` into a shared binding module so the other architectures could reuse them. Separately, `generation_utils::should_stop`'s `max_new_tokens`/stop-criteria handling — previously dead at every GPT-2 call site because the prompt boundary was passed as if it were the running sequence length — is fixed. **Two architectures went the opposite direction, from a fake "complete" to an honest non-complete, and are not part of the "resolved" list above**: a ~290-line `s4` loader that validated and skipped every tensor while still returning `Ok(())` is deleted outright rather than fixed (leaving `s4` with no loading path at all, corrected count updated throughout this file and `trustformers-models`'s own docs); `deepseek_v2` now returns a documented structured error instead of reporting fake success on any non-empty buffer (still not "complete" — see P1 above and `trustformers-models/TODO.md`).
- **`trustformers-core`'s Metal buffer lifetime**: `Tensor::Metal` now stores a `MetalBufferHandle` (RAII, refcounted) instead of a bare `BufferId`, and every GPU-to-GPU result site trustformers-core itself owns was converted to use it (`gelu`, `Tensor::add`, all `layernorm_gpu_to_gpu` sites, `Linear::forward`, `tensor_to_f32`'s download read) — Metal buffers now release on last-reference-drop instead of staying live until an explicit cache eviction. The module's own "known limitation, deliberately deferred" doc comment, which described the old bare-`BufferId` behavior, is rewritten to describe the now-resolved state honestly. (`trustformers-models`'s 15 not-yet-converted call sites are the one place this migration is still incomplete — see P0 #2.)
- **`trustformers-core::layers::paged_attention`**'s query/key projections use `forward_ref` (borrowing) instead of `.clone()`-ing `hidden_states` into two of the three Q/K/V calls — the previous version of this file listed this as the last known clone-vs-borrow site of its kind (lines 832-833 then, 834-835 today after other edits shifted line numbers), with a bit-identity regression test comparing against the old cloning behavior. **`utils/weight_loading.rs`'s `f16_to_f32`** now delegates to `half::f16::from_bits(bits).to_f32()` instead of a third hand-rolled bit-manipulation routine, with a regression test naming the two other call sites (`quantization/ggml_advanced.rs`, `quantization/gguf_k_quants.rs`) it was previously able to silently drift from.
- **`trustformers-mobile`**: real `sysinfo`-backed metric collection replaces three sets of invented per-platform constants; `MobileMetricsSnapshot`'s metric families are now `Option` rather than always-populated; bottleneck detection, alerting, and health scoring — previously structurally incapable of reporting anything real — now work; ~4,800 lines of dead/duplicate scaffolding deleted (`mobile-audit`'s own figure, not independently recounted); battery telemetry and `react-native-plugin/`→`react-native-example/` as described above; the device farm no longer invents device catalogues; a LoRA-A initialization fix (scaled by `1/sqrt(fan_in)`) resolved two genuinely flaky loss tests.
- **`trustformers-py`**: was fully broken before this wave (`py-pipelines`'s first item was repairing the crate so it compiled at all). Real inference now backs the `text-generation` and `text-classification` pipelines; `token-classification`/`question-answering` refuse with a structured error instead of inventing results (see P1 above); a pure `TokenizedInput` builder and a pure decoding core are shared between `GPT2LMHeadModel.generate()` and the pipeline layer; `AutoTokenizer`/`from_pretrained`/`save_pretrained` round-trip for real instead of fabricating a successful load; `src/models/losses.rs` (292 lines, 11 tests) computes real, closed-form-verified classification and language-modeling cross-entropy loss.
- **`trustformers` umbrella**: several dead-code items behind accessor gaps were closed by wiring them to real data instead of deleting the underlying (real) functionality — `ConfigurationManager::unrecognized_env_vars()`, `Mamba2Model::models_config()`/`layers()`, `OpenVINOModel::wrapper()` plus 5 wrapper accessors, `DdimScheduler::beta_range()`, `ObjectDetectionPipeline::labels()`/`resolve_label()`, `uptime()`/`advisor()`/`metrics_tracker()` accessors (with fabricated `expected_improvement` percentages removed alongside); `document_cache` is now wired for real cross-hop dedup; `djb2_hash` is now wired into `GroundingProcessor::encode_phrase`; several structurally dead scaffolding types (`DocumentRegion`/`RegionType`, a `StateManager` struct, a `performance_tracker` field) were deleted rather than kept unused; `generate_with_beam_search` and `format_cache_cleanup_start_message` are now correctly feature-gated (`t5`, `hub`) instead of always compiled; `serde_yaml` → `serde_yaml_ng`, verified above under Dependency hygiene.
- `trustformers-optim`'s `save_state`/`load_state`/`AdamW4bit`/stub-file claims in the old P2 list turned out to already be resolved (or never true) — see the corrected P2 entry above.
- `trustformers/examples/conversational_ai.rs.disabled` is enabled (renamed, no `.disabled` suffix) — see P2 above.
- `trustformers-mobile/src/tflite_nnapi_delegate.rs` is mounted from `lib.rs` — see the trustformers-mobile section above.
- **`trustformers-core/TODO.md`'s "Hardware Acceleration" checklist was itself a major fabrication, now corrected there.** Found while verifying the XLA/oneAPI doc-comment item above and checked further: that file's ROCm/oneAPI/XLA/TPU/RISC-V/Vulkan sections carried elaborate all-✅ checklists — "RVV 1.0 Compliance: Full specification support", "TPU v2: 180 teraflops, 64GB HBM" hardware specs, "DPC++ SYCL... oneDNN Integration... oneMKL", "Google XLA: HLO Compilation... Multi-Platform... TPU backend for Google Cloud" — none of which exist (XLA/oneAPI/RISC-V are `= []` feature flags with either a scalar-CPU fallback or "no runtime linked" errors; TPU has no feature flag or module at all; ROCm has real but hardware-unverified `dlopen`-based scaffolding; Vulkan has real device/pipeline setup but several compute methods return `not_implemented`). Root `README.md` already stated the true picture correctly ("ROCm, Vulkan, OpenCL are feature-gated and experimental", "TPU is a placeholder, not implemented") — only the crate-level `TODO.md` had drifted into fabrication. Rewritten there today with the verified per-backend detail; not duplicated here.

## Roadmap / not scheduled

Items below name no specific file, symbol, or acceptance criterion — they are directions, not actionable work items, and were previously mixed into "open work" lists in a way that inflated the apparent backlog. Kept here instead so the P0-P2 lists above stay honest about what's actually actionable. Several per-crate `TODO.md` files carry their own longer version of this list.

- More efficient architectures / "latest research architectures as they emerge" (no specific architecture or efficiency axis named)
- Enhanced memory pooling; better mobile quantization strategies (no target metric named)
- Interactive model architecture explorer
- Broader hardware backend coverage — moving ROCm/Vulkan/OpenCL from experimental/feature-gated to verified-on-real-hardware, and eventually giving RISC-V RVV / XLA / oneAPI real implementations instead of the empty feature flags tracked in P1
- `trustformers-tokenizers/docs/migration/`: the tiktoken guide was rewritten this pass to describe only the real API (see its own file); the other five migration guides (huggingface, sentencepiece, nltk, spacy, fairseq) and the migration `README.md` were found to carry the same class of problem at smaller scale — fabricated method/type names describing features this crate does not have (a rough same-pattern grep found roughly 12, 9, 2, 2, and 0 hits respectively, plus 1 in the migration `README.md`; tiktoken had ~16 before its rewrite). Each now carries a dated accuracy banner pointing this out rather than a full rewrite; a full pass through each is not scheduled.

## Per-crate detail

See each crate's own `TODO.md` for crate-specific open items: [trustformers-core](trustformers-core/TODO.md) · [trustformers-tokenizers](trustformers-tokenizers/TODO.md) · [trustformers-models](trustformers-models/TODO.md) · [trustformers-optim](trustformers-optim/TODO.md) · [trustformers-training](trustformers-training/TODO.md) · [trustformers](trustformers/TODO.md) (umbrella) · [trustformers-wasm](trustformers-wasm/TODO.md) · [trustformers-mobile](trustformers-mobile/TODO.md) · [trustformers-debug](trustformers-debug/TODO.md) · [trustformers-serve](trustformers-serve/TODO.md) · [trustformers-py](trustformers-py/TODO.md) · [trustformers-js](trustformers-js/TODO.md) (WASM/JS bindings; excluded, not a Cargo package). See also [TODO-CUDA.md](TODO-CUDA.md) for CUDA-specific tracking (cross-referenced from `trustformers-core/TODO.md`; keep the CUDA-6 item in exactly one of the two files, not both).

`trustformers-c` is **deprecated as of the 0.2.0 release** — zero maintenance effort, does not need to compile, and its own `TODO.md` states no new items will be tracked there going forward. Do not carry its open items into this file.

## Build commands

```bash
# Full workspace check (default features)
cargo check --workspace --all-targets

# Run tests for a specific crate (safer than a full workspace run while
# other agents may be mid-edit in a multi-agent session)
cargo nextest run -p <crate-name>

# Format / lint
cargo fmt --all
cargo clippy --workspace --all-targets -- -D warnings

# cargo-deny gates (see Status above for current pass/fail)
cargo deny check bans
cargo deny check licenses
cargo deny check advisories
```

## Development guidelines

- **Dependencies**: prefer `workspace = true` for every member dependency; `trustformers-serve` currently carries the largest count of literal version pins of any member (not audited pin-by-pin this pass — see P2). New dependencies must be pure Rust in default features; FFI dependencies (e.g. `mecab`, used optionally for Japanese/Korean tokenization) are feature-gated and never in `default`.
- **Code quality**: no `.unwrap()`/`.expect()` outside tests; snake_case naming; files under 2000 lines (see P1 File-size policy for current exceptions); zero-warning `clippy -D warnings` in the area you touch.
- **Testing**: every behavioral fix needs a regression test that would fail against the old code (this pattern is well-established across the codebase — see "Recently resolved" above for examples). Tests use `std::env::temp_dir()`, never a hardcoded path. No network access in tests; local mock servers are fine.
- **Banned crates**: openblas→oxiblas, bincode→oxicode, rustfft→oxifft, rusqlite→oxisql-sqlite-compat, zip/flate2/zstd/bzip2/lz4/tar/snap/brotli/miniz_oxide→oxiarc-\*, most `-sys` FFI crates→the matching cool-japan/oxi\* crate. Enforced by `deny.toml`'s `[bans]` table; run `cargo deny check bans` before finishing any dependency change.
